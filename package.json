{
  "private": true,
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "icto_v2",
  "scripts": {
    "dev": "npm run dev --workspace=frontend && node setupEnv.js",
    "build": "npm run build --workspace=frontend && node setupEnv.js",
    "prebuild": "npm run prebuild --workspaces --if-present",
    "pretest": "npm run prebuild --workspaces --if-present",
    "start": "npm start --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "type": "module",
  "workspaces": [
    "src/frontend"
  ],
  "dependencies": {
    "@dfinity/agent": "^2.4.1",
    "@dfinity/auth-client": "^2.4.1",
    "@dfinity/candid": "^2.4.1",
    "@dfinity/identity": "^2.4.1",
    "@dfinity/ledger-icp": "^3.0.0",
    "@dfinity/ledger-icrc": "^2.9.0",
    "@dfinity/nns": "^8.5.0",
    "@dfinity/principal": "^2.4.1",
    "@dfinity/utils": "^2.13.1",
    "@windoge98/plug-n-play": "^0.0.70",
    "bignumber.js": "^9.3.0",
    "lucide-vue-next": "^0.525.0",
    "vite-plugin-environment": "^1.1.3",
    "vue-sonner": "^2.0.1"
  }
}
