#!/usr/bin/env bash

# ==========================================
# Test Launchpad Configuration
# ==========================================

# Network settings
NETWORK="local"
BACKEND_CANISTER="backend"
ICP_LEDGER="icp_ledger"

# Sale Timeline (in seconds from now)
SALE_START_DELAY=120        # Start sale after 2 minutes
SALE_DURATION=300          # Sale lasts 5 minutes
CLAIM_START_DELAY=10       # Claim starts 10s after sale ends

# Sale Parameters (ICP - human readable, will be converted to e8s)
SOFTCAP_ICP=10              # 1 ICP soft cap
HARDCAP_ICP=50              # 5 ICP hard cap (5 participants x 1 ICP each)
TOKEN_PRICE=10000000       # 0.1 ICP per token (10000000 e8s = 0.1 ICP)
MIN_CONTRIBUTION_ICP=1   # 0.1 ICP minimum

# Token Supply (with 8 decimals)
TOTAL_TOKEN_SUPPLY=10000000000000000  # 100M tokens (100_000_000.00000000)

# Distribution Percentages (must sum to 100)
DIST_SALE=25
DIST_TEAM=20
DIST_LIQUIDITY=20
DIST_TREASURY=35

# Participant Settings (for launchpad-participate.sh)
DEFAULT_NUM_PARTICIPANTS=5
DEFAULT_ICP_PER_PARTICIPANT=1.0  # Each participant contributes 1 ICP

